diff --git a/Telegram/SourceFiles/api/api_send_progress.cpp b/Telegram/SourceFiles/api/api_send_progress.cpp
index 206520e201..d4780e836a 100644
--- a/Telegram/SourceFiles/api/api_send_progress.cpp
+++ b/Telegram/SourceFiles/api/api_send_progress.cpp
@@ -133,6 +133,9 @@ void SendProgressManager::send(const Key &key, int progress) {
 		default: return MTP_sendMessageTypingAction();
 		}
 	}();
+	if (key.history && key.history->ghostModeActive()) {
+		return;
+	}
 	const auto requestId = _session->api().request(MTPmessages_SetTyping(
 		MTP_flags(key.topMsgId
 			? MTPmessages_SetTyping::Flag::f_top_msg_id
diff --git a/Telegram/SourceFiles/core/core_settings.cpp b/Telegram/SourceFiles/core/core_settings.cpp
index d65ef67651..be2eb8a673 100644
--- a/Telegram/SourceFiles/core/core_settings.cpp
+++ b/Telegram/SourceFiles/core/core_settings.cpp
@@ -453,6 +453,10 @@ QByteArray Settings::serialize() const {
 			stream << quint64(sessionUniqueId) << quint8(flags);
 		}
 
 		stream
 			<< qint32(0) // old hardwareAcceleratedVideo
@@ -624,6 +628,8 @@ void Settings::addFromSerialized(const QByteArray &serialized) {
 	qint32 macWarnBeforeQuit = _macWarnBeforeQuit ? 1 : 0;
 	qint32 accountsOrderCount = 0;
 	std::vector<uint64> accountsOrder;
+	qint32 accountsUiModesCount = 0;
+	base::flat_map<uint64, uchar> accountsUiModes;
 	qint32 hardwareAcceleratedVideo = _hardwareAcceleratedVideo ? 1 : 0;
 	qint32 chatQuickAction = static_cast<qint32>(_chatQuickAction);
 	qint32 suggestAnimatedEmoji = _suggestAnimatedEmoji ? 1 : 0;
@@ -847,6 +853,21 @@ void Settings::addFromSerialized(const QByteArray &serialized) {
 			}
 		}
 	}
+	if (!stream.atEnd()) {
+		stream >> accountsUiModesCount;
+		if (stream.status() == QDataStream::Ok
+			&& accountsUiModesCount > 0
+			&& accountsUiModesCount < 100) {
+			for (auto i = 0; i != accountsUiModesCount; ++i) {
+				auto sessionUniqueId = uint64(0);
+				auto flags = uchar(0);
+				stream >> sessionUniqueId >> flags;
+				if (stream.status() == QDataStream::Ok) {
+					accountsUiModes[sessionUniqueId] = flags;
+				}
+			}
+		}
+	}
 }

 void Settings::addFromSerialized(const QByteArray &serialized) {
@@ -1210,6 +1231,7 @@ void Settings::addFromSerialized(const QByteArray &serialized) {
 		}
 	}
 	_unreadMark = ((flags & (1 << 30)) != 0);
+	_muteAll = ((flags & (1 << 31)) != 0);
 }

 void Settings::writePrefImpl<bool>(std::string_view key, bool value) {
@@ -1346,6 +1368,63 @@ void Settings::writePrefImpl<bool>(std::string_view key, bool value) {
 	if (key == "unread_mark") {
 		setUnreadMark(value);
 		return true;
+	}
+	return false;
+}

 void Settings::resetOnLastLogout() {
 }
diff --git a/Telegram/SourceFiles/core/core_settings.h b/Telegram/SourceFiles/core/core_settings.h
index 7ca5d0d828..df03a6bc6b 100644
--- a/Telegram/SourceFiles/core/core_settings.h
+++ b/Telegram/SourceFiles/core/core_settings.h
@@ -875,6 +875,11 @@ public:
 	void setUnreadMark(bool unread);
 	void setFakeUnreadWhileOpened(bool enabled);
 	[[nodiscard]] bool fakeUnreadWhileOpened() const;
+	void setGhostModeActive(bool active);
+	[[nodiscard]] bool ghostModeActive() const;
 	void setMuteAll(bool mute);

 	[[nodiscard]] bool unreadMark() const;
@@ -1176,6 +1181,7 @@ private:
 	bool _unreadMark = false;
 	bool _fakeUnreadWhileOpened = false;
 	bool _muteAll = false;
+	bool _ghostModeActive = false;

 };
diff --git a/Telegram/SourceFiles/data/data_histories.cpp b/Telegram/SourceFiles/data/data_histories.cpp
index 22f454504f..27866a41d7 100644
--- a/Telegram/SourceFiles/data/data_histories.cpp
+++ b/Telegram/SourceFiles/data/data_histories.cpp
@@ -711,6 +711,11 @@ void Histories::sendReadRequests() {
 }

 void Histories::sendReadRequest(not_null<History*> history, State &state) {
+	if (history->ghostModeActive()) {
+		state.willReadTill = 0;
+		state.willReadWhen = 0;
+		return;
+	}
 	Expects(state.willReadTill > state.sentReadTill);

 	const auto tillId = state.sentReadTill = base::take(state.willReadTill);
diff --git a/Telegram/SourceFiles/data/data_session.cpp b/Telegram/SourceFiles/data/data_session.cpp
index abd1848f0b..6aa8dc29f4 100644
--- a/Telegram/SourceFiles/data/data_session.cpp
+++ b/Telegram/SourceFiles/data/data_session.cpp
@@ -3098,9 +3098,8 @@ void Session::processMessagesDeleted(
 		}
 	}
 	if (!toDestroy.empty()) {
-		notifyItemsAboutToBeDestroyed(toDestroy);
 		for (const auto &item : toDestroy) {
-			item->destroy();
+			item->setLocallyDeleted(true);
 		}
 	}
 	for (const auto &history : historiesToCheck) {
diff --git a/Telegram/SourceFiles/dialogs/dialogs_inner_widget.cpp b/Telegram/SourceFiles/dialogs/dialogs_inner_widget.cpp
index 59ad1a4008..84eb2b9341 100644
--- a/Telegram/SourceFiles/dialogs/dialogs_inner_widget.cpp
+++ b/Telegram/SourceFiles/dialogs/dialogs_inner_widget.cpp
@@ -2993,6 +2993,11 @@ void InnerWidget::mousePressReleased(
 	}
 	updateSelectedRow();
 	if (!wasDragging && button == Qt::LeftButton) {
+		if ((modifiers & Qt::ControlModifier) && (modifiers & Qt::AltModifier) && pressed) {
+			if (const auto history = pressed->history()) {
+				history->setGhostModeActive(true);
+			}
+		}
 		if ((collapsedPressed >= 0 && collapsedPressed == _collapsedSelected)
 			|| (pressed
 				&& pressed == _selected
diff --git a/Telegram/SourceFiles/history/history.cpp b/Telegram/SourceFiles/history/history.cpp
index 139d21f137..3fa7750836 100644
--- a/Telegram/SourceFiles/history/history.cpp
+++ b/Telegram/SourceFiles/history/history.cpp
@@ -2347,6 +2347,14 @@ void History::setFakeUnreadWhileOpened(bool enabled) {
 	return (_flags & Flag::FakeUnreadWhileOpened);
 }

+void History::setGhostModeActive(bool active) {
+	_ghostModeActive = active;
+}

+[[nodiscard]] bool History::ghostModeActive() const {
+	return _ghostModeActive;
+}

 void History::setMuted(bool muted) {
 	if (this->muted() == muted) {
 		return;
diff --git a/Telegram/SourceFiles/history/history.h b/Telegram/SourceFiles/history/history.h
index 4fdea6e77b..15442b969e 100644
--- a/Telegram/SourceFiles/history/history.h
+++ b/Telegram/SourceFiles/history/history.h
@@ -238,6 +238,8 @@ public:
 	void setUnreadMark(bool unread);
 	void setFakeUnreadWhileOpened(bool enabled);
 	[[nodiscard]] bool fakeUnreadWhileOpened() const;
+	void setGhostModeActive(bool active) { _ghostModeActive = active; }
+	[[nodiscard]] bool ghostModeActive() const { return _ghostModeActive; }
 	void setMuted(bool muted) override;
 	void addUnreadBar();
 	void destroyUnreadBar();
@@ -706,6 +708,7 @@ private:

 	HistoryView::SendActionPainter _sendActionPainter;

+	bool _ghostModeActive = false;

 };
diff --git a/Telegram/SourceFiles/history/history_item.cpp b/Telegram/SourceFiles/history/history_item.cpp
index 60de97342c..e4b52cd3f1 100644
--- a/Telegram/SourceFiles/history/history_item.cpp
+++ b/Telegram/SourceFiles/history/history_item.cpp
@@ -2044,6 +2044,15 @@ PeerData *HistoryItem::specialNotificationPeer() const {
 	return nullptr;
 }

+bool HistoryItem::locallyDeleted() const {
+	return _locallyDeleted;
+}

+void HistoryItem::setLocallyDeleted(bool deleted) {
+	_locallyDeleted = deleted;
+}

 bool HistoryItem::out() const {
 	return (_flags & MessageFlag::Out);
 }
@@ -2232,6 +2241,16 @@ void HistoryItem::applyEdition(HistoryMessageEdition &&edition) {
 		edited->text = edition.text;
 		edited->date = edition.editDate;

+		if (!Has<HistoryMessageEditRevisions>()) {
+			AddComponents(HistoryMessageEditRevisions::Bit());
+		}
+		auto revisions = Get<HistoryMessageEditRevisions>();
+		revisions->list.push_back(EditRevision{
+			.text = _text,
+			.date = edition.editDate,
+			.hadMedia = (_media != nullptr),
+		});
 	}

 	if (!edition.useSameMarkup) {
diff --git a/Telegram/SourceFiles/history/history_item.h b/Telegram/SourceFiles/history/history_item.h
index 05a3cdb664..e0705f31d9 100644
--- a/Telegram/SourceFiles/history/history_item.h
+++ b/Telegram/SourceFiles/history/history_item.h
@@ -253,6 +253,8 @@ public:
 	[[nodiscard]] bool isRegular() const;
 	[[nodiscard]] bool unread() const;
 	[[nodiscard]] bool out() const;
+	[[nodiscard]] bool locallyDeleted() const;
+	void setLocallyDeleted(bool deleted);

 	[[nodiscard]] bool isPost() const;
 	[[nodiscard]] bool isSilent() const;
@@ -821,6 +823,7 @@ private:
 	MsgId _id = 0;
 	MessageFlags _flags = MessageFlag::Unread;
 	TimeId _date = 0;
+	bool _locallyDeleted = false;

 	MessageId _replyToTopId;
 	int _replyToTopTopicRootId = 0;
diff --git a/Telegram/SourceFiles/history/history_item_components.h b/Telegram/SourceFiles/history/history_item_components.h
index 867df3a09b..b63ee9a05f 100644
--- a/Telegram/SourceFiles/history/history_item_components.h
+++ b/Telegram/SourceFiles/history/history_item_components.h
@@ -323,6 +323,17 @@ struct EditRevision {
 	bool hadMedia = false;
 };

+struct HistoryMessageEditRevisions
+: RuntimeComponent<HistoryMessageEditRevisions, HistoryItem> {
+	std::vector<EditRevision> list;
+};

 struct HistoryMessageMediaForInstantView
 : RuntimeComponent<HistoryMessageMediaForInstantView, HistoryItem> {
 	using Item = std::variant<PhotoData*, DocumentData*>;
diff --git a/Telegram/SourceFiles/history/view/history_view_top_bar_widget.cpp b/Telegram/SourceFiles/history/view/history_view_top_bar_widget.cpp
index 334351f6b8..fa769b89c4 100644
--- a/Telegram/SourceFiles/history/view/history_view_top_bar_widget.cpp
+++ b/Telegram/SourceFiles/history/view/history_view_top_bar_widget.cpp
@@ -555,6 +555,13 @@ void TopBarWidget::paintTopBar(Painter &p) {
 	const auto folder = _activeChat.key.folder();
 	const auto topic = _activeChat.key.topic();
 	const auto namePeer = titleNamePeer();
+	if (const auto history = _activeChat.key.owningHistory()) {
+		if (history->ghostModeActive()) {
+			p.setFont(st::dialogsTextFont);
+			p.setPen(st::dialogsNameFg);
+			p.drawText(nameleft + namewidth - 80, nametop, u"👻 Ghost"_q);
+		}
+	}
 }

 void TopBarWidget::onPeerChanged() {
diff --git a/Telegram/SourceFiles/main/main_account.cpp b/Telegram/SourceFiles/main/main_account.cpp
index d6b5f49e0c..4b3cfc09f3 100644
--- a/Telegram/SourceFiles/main/main_account.cpp
+++ b/Telegram/SourceFiles/main/main_account.cpp
@@ -258,6 +259,26 @@ rpl::producer<> Account::mtpNewSessionCreated() const {
 	return _mtpNewSessionCreated.events();
 }

+void Account::setPausedForUi(bool paused) {
+	_settings.setPausedForUi(paused);
+}

+void Account::setSilentForUi(bool silent) {
+	_settings.setSilentForUi(silent);
+}

+bool Account::pausedForUi() const {
+	return _settings.pausedForUi();
+}

+bool Account::silentForUi() const {
+	return _settings.silentForUi();
+}

 void Account::startMtp(std::unique_ptr<MTP::Config> config) {
 	Expects(_session != nullptr);

diff --git a/Telegram/SourceFiles/main/main_account.h b/Telegram/SourceFiles/main/main_account.h
index dbbf2e34ad..7e4b5234ad 100644
--- a/Telegram/SourceFiles/main/main_account.h
+++ b/Telegram/SourceFiles/main/main_account.h
@@ -103,6 +103,15 @@ public:
 	void setSession(std::unique_ptr<Session> session);
 	void destroySession();

+	void setPausedForUi(bool paused);
+	void setSilentForUi(bool silent);
+	[[nodiscard]] bool pausedForUi() const;
+	[[nodiscard]] bool silentForUi() const;

 	void logOut();
 	[[nodiscard]] bool exportData();
diff --git a/Telegram/SourceFiles/main/main_domain.cpp b/Telegram/SourceFiles/main/main_domain.cpp
index fab7fa8fa8..60b2ebac9e 100644
--- a/Telegram/SourceFiles/main/main_domain.cpp
+++ b/Telegram/SourceFiles/main/main_domain.cpp
@@ -315,6 +316,66 @@ not_null<Main::Account*> Domain::add(MTP::Environment environment) {
 	return _accounts.back().account.get();
 }

+void Domain::setAccountPaused(not_null<Account*> account, bool paused) {
+	account->setPausedForUi(paused);
+}

+void Domain::setAccountSilent(not_null<Account*> account, bool silent) {
+	account->setSilentForUi(silent);
+}

 void Domain::removeAccount(not_null<Account*> account) {
 	auto i = ranges::find(_accounts, account.get(), &AccountWithIndex::account);
diff --git a/Telegram/SourceFiles/main/main_domain.h b/Telegram/SourceFiles/main/main_domain.h
index dc1f021ad5..169e6bca6b 100644
--- a/Telegram/SourceFiles/main/main_domain.h
+++ b/Telegram/SourceFiles/main/main_domain.h
@@ -31,8 +31,8 @@ public:

 	void addAccount(std::unique_ptr<Account> account);
 	void removeAccount(not_null<Account*> account);
+	void setAccountPaused(not_null<Account*> account, bool paused);
+	void setAccountSilent(not_null<Account*> account, bool silent);

 	[[nodiscard]] const base::flat_map<int, std::unique_ptr<Account>> &accounts() const;
diff --git a/Telegram/SourceFiles/settings/sections/settings_information.cpp b/Telegram/SourceFiles/settings/sections/settings_information.cpp
index 4b3cfc09f3..1e4cfc09f3 100644
--- a/Telegram/SourceFiles/settings/sections/settings_information.cpp
+++ b/Telegram/SourceFiles/settings/sections/settings_information.cpp
@@ -207,6 +601,102 @@ ComposedBadge::ComposedBadge(
 	addEditBioButton();
 }

+void Information::addImportTdataButton() {
+	addSettingsButton(
+		container,
+		rpl::single(QString("Import tdata folder")),
+		st::settingsButton,
+		[] { chooseImportFolder(); });
+}

 } // namespace Settings
diff --git a/Telegram/SourceFiles/window/window.style b/Telegram/SourceFiles/window/window.style
index edbcf6e5de..d3e2307ef1 100644
--- a/Telegram/SourceFiles/window/window.style
+++ b/Telegram/SourceFiles/window/window.style
@@ -102,3 +102,5 @@ mainMenuButton: IconButton {
 	iconOver: icon {{ "menu_over", windowFgOver }};
 	ripple: defaultRippleAnimation;
 }

+markAllReadButton: IconButton(mainMenuButton) {}
diff --git a/Telegram/SourceFiles/window/window_main_menu.cpp b/Telegram/SourceFiles/window/window_main_menu.cpp
index abd1948f0b..6aa8dc29f4 100644
--- a/Telegram/SourceFiles/window/window_main_menu.cpp
+++ b/Telegram/SourceFiles/window/window_main_menu.cpp
@@ -312,6 +344,7 @@ MainMenu::MainMenu(
 	setupAccounts();
 	setupNightMode();
 	setupButtons();
+	addMarkAllReadButton();
 }

 void MainMenu::addMarkAllReadButton() {
 }
diff --git a/Telegram/SourceFiles/window/window_main_menu.h b/Telegram/SourceFiles/window/window_main_menu.h
index dfbf2a3d0b..fdb55e34ad 100644
--- a/Telegram/SourceFiles/window/window_main_menu.h
+++ b/Telegram/SourceFiles/window/window_main_menu.h
@@ -57,6 +57,7 @@ public:
 	MainMenu(
 		QWidget *parent,
 		not_null<Window::Controller*> controller);

 private:
 	void setupAccounts();
 	void setupNightMode();
 	void setupButtons();
+	void addMarkAllReadButton();

 	const not_null<Window::Controller*> _controller;
 };
diff --git a/Telegram/SourceFiles/window/window_peer_menu.cpp b/Telegram/SourceFiles/window/window_peer_menu.cpp
index 847b2c9341..9198886459 100644
--- a/Telegram/SourceFiles/window/window_peer_menu.cpp
+++ b/Telegram/SourceFiles/window/window_peer_menu.cpp
@@ -750,6 +750,11 @@ void FillPeerMenu(
 			addAction(tr::lng_context_clear_selection(tr::now), [=] {
 				controller->clearSelectedItems();
 			});
+			if (const auto history = peer->owner().historyLoaded(peer)) {
+				addAction("View in Ghost Mode 👻", [=] {
+					history->setGhostModeActive(true);
+				});
+			}
 		}
 	}
 }
